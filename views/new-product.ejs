<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= headTitle %></title>
    <link rel="stylesheet" href="/css/main.css" />
  </head>
  <body>
    <%- include('partials/navigation', { menuLinks: menuLinks, activeLinkPath:
    activeLinkPath }); %>
    <main>
      <h1 class="heading">Newest product</h1>
      <% if (newestProduct && Object.keys(newestProduct).length > 0) { %>
      <div>
        New product data - Name: <%= newestProduct.name %>, Description: <%=
        newestProduct.description %>
      </div>
      <% } else { %>
      <div>No data available</div>
      <% } %>
    </main>
  </body>
</html>
